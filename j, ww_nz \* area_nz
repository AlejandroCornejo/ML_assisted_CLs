116:def _AssembleHyperReducedSystemKratosLocal(mp, n_dof, elements, elem_indices, elem_weights):
143:        rhs[ids_m] += w_e * np.array(RHS, dtype=float)[mask]
145:        A = w_e * np.array(LHS, dtype=float)[np.ix_(mask, mask)]
157:def AssembleHyperReducedSystem(mp, n_dof, elements, elem_indices, elem_weights, u_eq=None):
294:def ResolveResidualHyperReductionSelection(ecm_data, n_current_elements, solver_label="HPROM"):
361:def ResolveHomogenizationWeightSelection(ecm_data, n_current_elements, solver_label="HPROM"):
393:def CalculateHomogenizedFromAssemblerWithElementWeights(assembler, w_eps=None, w_sig=None, reference_measure=None):
397:      value_hom = (sum_e A_e * mean_gp(value_gp,e)) / A_ref
