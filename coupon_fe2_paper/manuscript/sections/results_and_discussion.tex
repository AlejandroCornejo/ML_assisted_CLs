\newcommand{\pendingresult}[1]{\begin{quote}\small\color{blue!45!black}\textbf{Draft task---evidence pending.} #1\end{quote}}

\section{Constitutive assessment on periodic microstructures}
\label{sec:cell}
In this section, we evaluate the proposed constitutive modeling approach on two periodic representative volume elements (RVEs) with different directional microstructures. Material A contains one elliptical cavity rotated relative to the cell axes; material B contains four elliptical cavities of different sizes and orientations. Both use the same isotropic matrix law and $20\%$ porosity. Together they let us assess the learned energy in a cell with one prominent geometric direction and in a cell with several. We compare the surrogates with independent full-order RVE responses and, in material B, assess the effect of learning the paired-feature parameters against fixed-feature counterparts. The material-A models are subsequently deployed in the structural FE$^2$ calculation of Section~\ref{sec:coupon}. We distinguish the guarantees established by construction from the numerical accuracy and consistency checks reported here.

\subsection{Microstructures and reference-model verification}
\subsubsection{Material A: single-cavity reference}
\label{sec:material_a}
The square periodic cell contains a centered elliptical void with area fraction $0.20$, aspect ratio $2$, and major-axis angle $30^\circ$ relative to the cell axes; see Fig.~\ref{fig:rve}. This orientation avoids the mirror-aligned configurations of the square cell. Centrosymmetry remains, but does not mean that the strain energy is an even function of strain.

The solid is compressible Neo-Hookean in plane strain. With $\C_\mu=\F_\mu^T\F_\mu$ and $J_\mu=\det\F_\mu>0$, its local energy density is
\begin{equation}
 \psi_\mu=\frac\mu2(\tr\C_\mu-2)-\mu\log J_\mu
              +\frac\lambda2(\log J_\mu)^2,
\end{equation}
with Young's modulus $1.628$ GPa and Poisson ratio $0.4$. Here $\mu$ and $\lambda$ are the usual Lam\'e constants. This is the specific matrix law used by the reference solver, not a generic assertion about every Neo-Hookean variant. The production mesh contains 1546 quadratic triangles, 3240 nodes, and 4638 quadrature points. Periodicity and null-space constraints leave 6320 independent microscopic displacement coordinates. Cell averages use the complete reference cell measure.

\begin{figure}[htbp]
 \centering\includegraphics[width=.96\linewidth]{rve_geometry.pdf}
 \caption{Material A: cavity geometry (left) and working quadratic mesh (right). Dashed lines mark cavity major axes; signed angles are measured counterclockwise from $X_1$. Only edges joining corner nodes are drawn, and $\ell$ denotes the cell side.}
 \label{fig:rve}
\end{figure}

The production mesh was selected using a local stress-field criterion along a uniaxial-stress path, rather than only a homogenized average. On this path, $E_{22}$ and $2E_{12}$ are solved so that $\overline S_{22}=\overline S_{12}=0$. At $E_{11}=0.20$, its maximum microscopic first Piola stress norm differs by approximately $0.486\%$ from a 3095-element reference, whereas the average axial stress differs by approximately $0.001\%$. This is a mesh check at a specified state, not a uniform error bound over all multiaxial data.

\subsubsection{Material B: multicavity reference}
\label{sec:material_b}
Material B retains the square periodic cell, the plane-strain Neo-Hookean matrix law and moduli, and the $20\%$ total void fraction of material A. The void space is instead distributed among four ellipses with unequal sizes, aspect ratios, orientations, and off-center positions; see Fig.~\ref{fig:rve_b}. Holding the matrix and porosity fixed isolates the change in cavity arrangement. Unlike the single rotated void in A, this cell presents several geometric directions within the same microstructure. We do not assign a material symmetry group to the resulting effective law.

The working mesh contains 4621 quadratic triangles and 9582 nodes. Periodic boundary conditions and full-cell averaging follow Section~\ref{sec:fom}.

\begin{figure}[htbp]
 \centering\includegraphics[width=.96\linewidth]{rve_geometry_b.pdf}
 \caption{Material B: cavity geometry (left) and working quadratic mesh (right). Dashed lines mark cavity major axes; signed angles are measured counterclockwise from $X_1$. Only edges joining corner nodes are drawn, and $\ell$ denotes the cell side.}
 \label{fig:rve_b}
\end{figure}

At 64 prescribed multiaxial states, the working mesh was compared with an 8961-element mesh. Relative differences in homogenized energy, stress, and tangent remained below $0.1\%$; the sampled maximum microscopic first Piola stress norm differed by at most $4.06\%$. These finite comparisons support the working mesh, not a uniform convergence bound.

\FloatBarrier
\subsection{Data domains and learning protocol}
\label{sec:constitutive_training}
\paragraph{Training domains and data roles.}
To define the training domain for material A, we first consider the problem it must solve. In Section~\ref{sec:coupon}, its learned law supplies the constitutive response throughout a coupon FE$^2$ calculation, where different macroscopic quadrature points visit different strains as loading proceeds. To locate those strains before training, we ran a displacement-controlled coupon prepass with a St Venant--Kirchhoff approximation based on the cell's reference tangent. The prepass recorded the strain at every macroscopic quadrature point; it defined the sampling envelope, not the stresses used to train the models. Displacement control prevented errors in the approximate force response from determining that envelope. A second prepass with a softer response shifted the observed componentwise extrema by at most $5.1\%$; we expanded the spans by $40\%$ to allow for such differences. Because the coupon is loaded in tension, we set the lower $E_{11}$ bound to zero. In the engineering-strain coordinates $\ee=(E_{11},E_{22},2E_{12})^T$, the rounded bounds are
\begin{equation}
 \ee_{\min}=(0,-0.09865,-0.16807)^T,\qquad
 \ee_{\max}=(0.1955,0.01635,0.10793)^T.
 \label{eq:box}
\end{equation}
We sample this box on a Cartesian grid, which also provides neighboring states for the reduced models of Section~\ref{sec:reduced}. Of its 4950 nonreference states, 4208 are used for fitting and 742 for validation (split seed 5). An independent set of 400 off-grid states tests interpolation.

Material B serves a different purpose: it tests the constitutive models on a cell with several geometric directions and permits a controlled comparison of fixed and learned features. It is not used in the coupon FE$^2$ calculation or a reduced model, so its sampling domain need not inherit A's coupon-derived bounds or Cartesian grid. After full-order and mesh checks at multiaxial boundary states, we chose $E_{11},E_{22}\in[-0.04,0.20]$ and $2E_{12}\in[-0.08,0.08]$. This box includes moderate normal compression as well as extension and shear within the checked range. Low-discrepancy samples in its interior and on its faces and corners supply 4200 fitting states; separate samplings supply 512 validation and 512 test states. Ten prescribed loading paths are reserved for evaluation. Figure~\ref{fig:constitutive_domains} shows both sampling designs.

\begin{figure}[htbp]
 \centering\includegraphics[width=.98\linewidth]{constitutive_domains.pdf}
 \caption{Strain-sampling domains for Material A (left) and Material B (right), in percent. Wireframes show the prescribed limits; colors identify fit, validation, and independent test. For legibility, each panel displays 240 fit, 110 validation, and 110 test coordinates selected deterministically; full counts appear in the text.}
 \label{fig:constitutive_domains}
\end{figure}
\FloatBarrier

\paragraph{Optimization and model selection.}

For material A, each network represents one scalar energy. Its first strain derivative supplies stress, and its second strain derivative at the undeformed state supplies the reference tangent (initial stiffness); these are not independent outputs. We fit all three quantities against RVE data. Stress has the largest weight because it is the response queried throughout the FE$^2$ calculation. The loss combines dimensionless relative mean-square errors for stress, energy, and reference tangent, weighted $1$, $0.2$, and $0.02$, respectively. The tangent target comes from a separate coarse periodic RVE calculation.

Parameter fitting starts with 2600 full-batch Adam steps and ends with up to 400 L-BFGS iterations to refine the fit. Validation stress error determines which checkpoint is retained; the independent test and probe states are not consulted. The selected ICNN and ICKAN checkpoints both use random-initialization seed 16 and 32 paired features. Each has two hidden layers, with 24 nodes per layer for ICNN and 12 for ICKAN. Because the earlier baselines used different features, objectives, and training budgets, their comparison measures practical accuracy under those protocols, not compute-matched architectural superiority.

Before entering either neural core, each feature $z_i$ is centered on its undeformed value $z_i^0$ and divided by a positive scale $s_i$. The center is updated as the learned feature powers change; the scale $s_i=\max(10^{-4},\max_{\rm fit}|z_i-z_i^0|)$ is set once from the initial spread over fitting states. This keeps input magnitudes comparable during optimization without changing the constitutive constraints. For the selected ICKAN, each connection learns coefficients for six integrated-hat basis functions; their knot centers are fixed from $-1.2$ to $1.2$ at spacing $h=0.48$.

Material B supplies the controlled feature comparison absent from A. We trained Free and the fixed- and learned-feature versions of ICNN and ICKAN at seeds 16, 29, and 47. Within each core, fixed and learned variants share the 32-feature initialization, data, and objective. Checkpoints were selected by validation stress error and locked before test and loading-path evaluation. All runs used validation-plateau learning-rate scheduling and stopping, then L-BFGS, with a 200,000-step Adam cap. Free reached this cap in every seed without a plateau; its Adam stage is therefore budget-limited.

\subsection{Constitutive response and predictive accuracy}
\label{sec:constitutive_accuracy}
For material A, the selected ICNN and ICKAN models attain held-out aggregate stress errors of approximately $0.65\%$; Table~\ref{tab:constitutive} reports the independently audited selected checkpoints. Free has a corresponding test error of $1.2643\%$. This comparison concerns the archived training protocols, not a matched-budget architecture study. Outside the training domain, Free has a lower aggregate probe error than either polyconvex model; Supplementary Section~S1 preserves those results. Architectural admissibility does not guarantee extrapolation accuracy.

\begin{table}[htbp]
 \centering\small
 \caption{Material A: independent-test constitutive errors of the selected polyconvex models, in percent.}
 \label{tab:constitutive}
 \input{tables/constitutive_errors.tex}
\end{table}

\pendingresult{Add FOM/model stress curves on predeclared axial, shear and combined loading paths, followed by the corresponding independent-test results for B. Inventory existing evaluations before requesting new FOM solves; do not connect unrelated scattered states as though they were a loading path. Report componentwise behavior and variability where needed to interpret aggregate errors.}

\subsection{Contribution of learned paired features}
\label{sec:feature_ablation}
\pendingresult{Run a controlled comparison of fixed and learned admissible paired features, preferably for both A and B. Keep the core architecture, feature count, data, objective and optimization budget comparable; declare the fixed configuration and multiple initialization seeds before inspecting test errors. Retain model-specific reference normalization and growth terms. This comparison is not supplied by the archived baselines and has no results yet.}

\subsection{Mechanical consistency and scope}
\label{sec:mechanical_assessment}
The following numerical checks concern the available material-A checkpoints. They complement, rather than replace, the construction guarantees in Section~\ref{sec:learned}.

The archived independent audit records successful energy first- and second-derivative checks and saved-weight nonnegative-energy sufficient bounds for both selected models. The conservative finite-interval margins are $17.25$ and $16.82$ in normalized energy units. Random admissible-deformation tests support implementation consistency, but the polyconvexity argument is analytical. Neither aggregate error nor a successful bound on energy is a uniform bound on stress prediction error.

For the energy-based models, sampled rank-one curvatures provide an implementation check using the stress-dependent expression derived in Appendix~\ref{app:finite_strain_curvature}. The detailed sampling audit is retained in Supplementary Section~S2. These finite samples do not establish polyconvexity; the ICNN and ICKAN guarantee follows from the analytical construction.

Closed-cycle work consistency follows from the scalar potential for Free, ICNN, and ICKAN. A numerical audit of the material-A constitutive outputs is reported in Supplementary Section~S3.

\pendingresult{Repeat the reference-state, derivative, cycle and saved-parameter nonnegative-energy checks for the selected material-B models. Report bounds that fail or remain inconclusive. Existing A-checkpoint audits cannot certify B, and a second material trained separately does not demonstrate transfer between geometries.}

\section{Structural deployment for material A: accuracy and online cost}
\label{sec:coupon}
\subsection{Purpose, boundary conditions, and coverage}
We now use material A to compare learned constitutive laws and reduced microscopic solvers within the same nonlinear structural calculation. The coupon measures how their approximations affect structural accuracy and online cost. The dog-bone profile has overall length 165 mm, grip width 19 mm, narrow-section width 13 mm, narrow-section length 57 mm, and fillet radius 76 mm. It is a plane-strain computational benchmark using an ASTM-inspired in-plane profile, not a simulation certified to reproduce an ASTM tensile test.

The two end faces receive equal-and-opposite axial resultants of 100 kN per end, applied in 20 equal increments. Three point constraints remove rigid modes: the axial and transverse displacement at the left mid-edge node, and transverse displacement at the right mid-edge node. Entire end faces are not clamped. Consequently, a small skew of the deformed ends is not ruled out by the boundary conditions. The anisotropic response can couple axial extension to shear.

The macro mesh contains 690 quadratic triangles, 1501 nodes, and 2070 integration points. The reference extrusion thickness used to convert the two-dimensional stress integrals into the stated force resultants is 50 mm. This force convention is part of the plane-strain benchmark, not an ASTM specimen-thickness specification. The physical cell size is declared as 1.3 mm, corresponding to ten cells across the narrow width. This provides a scale-separation assumption, not a direct numerical validation of homogenization error at that ratio. All saved converged integration-point strains for every reported model remain inside the unchanged training box. This check does not automatically cover unsaved intermediate Newton iterates or certify another load case.

\begin{figure}[htbp]
 \centering\includegraphics[width=.99\linewidth]{coupon_fields.pdf}
 \caption{Nested FOM--FE$^2$ reference at 100 kN per end. Fields are displayed on the actual deformed mesh, without displacement amplification. Gauss-point strain and stress values are first averaged within each element and then patch-averaged to nodes for visualization only. These recovered contours are not the raw fields used in error norms and can smooth local extrema. Stresses are second Piola components. A three-dimensional von Mises stress is not reconstructed because the saved output does not include the necessary out-of-plane Cauchy stress.}
 \label{fig:coupon}
\end{figure}

\subsection{Reduced-state representation for material A}
\label{sec:material_a_reduction}
The 4950 displacement snapshots provide the basis of the reduced microscopic models as well as the effective constitutive labels. Figure~\ref{fig:pod} separates POD compression from the strain-informed coordinate fit. The relative snapshot truncation norm for the retained 39-dimensional space is $9.94\times10^{-7}$. After its orthogonal rotation into primary and secondary spaces, $n=3$ and $\bar n=36$ in the notation of Section~\ref{sec:reduced}.

The fitted relation between the deployed primary coordinates and macroscopic strain has an aggregate relative array discrepancy of $3.60\times10^{-5}$ on these snapshots; its maximum absolute component discrepancy is $2.55\times10^{-5}$. These quantities explain why macroscopic strain is a useful primary-coordinate predictor for this material. They do not imply exact equality at unsampled states, nor do they measure the closure error or the equilibrium residual. In particular, the direct substitution in D-HPROM--ANN is an additional approximation, even when the plot appears visually indistinguishable from the identity.

The secondary-coordinate closure is a $3$--$64$--$64$--$36$ feed-forward network with two $\tanh$ hidden layers and a linear output. Its training script uses input standardization, the raw secondary-coordinate loss in Eq.~\eqref{eq:closure_loss}, and a 90/10 internal fit/validation split with seed 20260903. Full-batch Adam has initial learning rate $2\times10^{-3}$, a validation-driven reduction schedule, and a maximum budget of 20000 epochs. The archived best relative validation mean-square error is $3.75\times10^{-7}$. This is an internal closure metric after a POD built from all 4950 snapshots, not an independent material-generalization estimate. Independent effective-stress and structural tests provide the latter assessment.

Each deployed adaptive cubature field has architecture $3$--$128$--$128$--$128$--$10$, GELU hidden activations, and the scaled softmax output of Eq.~\eqref{eq:softmax_weights}. The residual and stress checkpoints use distinct ten-element supports. Their larger neural cores illustrate why the online cost cannot be inferred solely from the three equilibrium coordinates or from the element count. These reduction-training choices differ from those of the constitutive networks; the common material and data domain do not imply matched offline training budgets.

\begin{figure}[htbp]
 \centering\includegraphics[width=.99\linewidth]{pod_primary_coordinates.pdf}
 \caption{Diagnostics from the deployed 39-mode basis artifact. Left: relative POD snapshot truncation norm, before the primary/secondary rotation. Right: strain-informed primary coordinates versus the corresponding macroscopic strains; every tenth snapshot is plotted, whereas the reported discrepancies use all 4950 states. Neither diagnostic measures structural accuracy or proves validity outside the sampled domain.}
 \label{fig:pod}
\end{figure}

\subsection{Online models and implementation verification}
The linear HPROM uses $n_{\rm tra}=39$ generalized coordinates, a residual rule supported on 135 elements, and a stress rule on 73 elements; their union contains 183 elements. The integrand-SVD tolerances are $\varepsilon_{\rm SVD}^{r}=10^{-3}$ and $\varepsilon_{\rm SVD}^{s}=10^{-5}$, retaining 134 and 72 integrand modes, respectively. The greedy integration tolerance in Eq.~\eqref{eq:ecm_tolerance} is $\varepsilon_{\rm ECM}=10^{-6}$ for both rules, including the multiplier-sum constraint.

The iterative HPROM--ANN uses $n=3$ primary and $\bar n=36$ secondary coordinates, with ten residual and ten stress elements and a union of 19. Initial fixed-ECM candidates use $\varepsilon_{\rm SVD}=10^{-4}$ and $\varepsilon_{\rm ECM}=10^{-6}$; adaptive pruning targets ten elements per rule. Thus this final count is a prescribed pruning target assessed through integration and response errors, not a consequence of the greedy tolerance alone. The support-generation driver uses up to 500 sampled states and tests up to 20 removal candidates per elimination. Its positivity-enforcing phase is active from the first elimination; the available driver does not enable or supply the global graph coupling. The residual weight field is subsequently refitted on the same pruned support with a longer training budget. Historical pruning diagnostics are not stored in the deployed checkpoints, so their filenames alone are not evidence of a graph-regularized run.

The direct model evaluates stress on ten elements and solves no microscopic equilibrium equations. All these counts refer to reduced computational meshes, not full-mesh evaluations followed by masking. Figure~\ref{fig:supports} shows their spatial distribution and the overlap of residual and output rules.

\begin{figure}[htbp]
 \centering\includegraphics[width=.99\linewidth]{reduced_supports.pdf}
 \caption{Element supports of the deployed hyperreduced microscopic models. The pale full-cell mesh provides location context only; discarded elements are not assembled online. Colors distinguish residual-only, stress-only, and shared elements. Supports are read from the same index arrays used to construct the reduced MDPA meshes.}
 \label{fig:supports}
\end{figure}

For the optimized linear and direct implementations, the maximum normalized stress differences from the original implementations over the verification states are $4.71\times10^{-14}$ and $4.20\times10^{-13}$. Corresponding tangent differences are $2.68\times10^{-10}$ and $7.25\times10^{-10}$. Independent finite-difference checks give discrepancies of approximately $3.51\times10^{-9}$ and $2.38\times10^{-9}$. Such comparisons check that optimization preserves the deployed model; they do not establish that either model exactly reproduces full microscopic equilibrium.

\subsection{Error and timing definitions}
For a saved nodal or integration-point array $A$, the reported relative error is
\begin{equation}
 e_A=100\frac{\|\operatorname{vec}(A-A_0)\|_2}{\|\operatorname{vec}(A_0)\|_2},
 \label{eq:error}
\end{equation}
where $A_0$ is the nested reference on the same meshes. These are unweighted discrete array norms. The strain array stores engineering shear, whereas the stress array stores tensor shear. They are neither quadrature-weighted continuum $L^2$ norms nor invariant tensor Frobenius norms. The metric is kept identical across models; this does not make the three different field errors interchangeable.

Online wall time includes the complete macroscopic solve, microscopic or surrogate stress/tangent evaluations, communication, and lazy worker construction within the timed solve. Parent setup, offline data generation, training, reduced-mesh construction outside the timed region, and result export are excluded. The FOM uses 20 worker processes with one numerical-library thread per worker. The three optimized microscopic surrogates use the same process configuration and three sequential fresh complete runs. PANN evaluations are batched in one process and use the separately investigated thread settings shown in Table~\ref{tab:fe2}.

The macroscopic solve stops when the free-degree-of-freedom residual norm falls below $10^{-5}$ N or its ratio to the initial increment residual falls below $10^{-7}$. The periodic FOM uses a relative microscopic displacement-increment tolerance of $10^{-10}$, with continuation when needed. The archived FOM and selected PANN times each have one repetition in this comparison. Thus their ratios are observed online speedups under the stated implementations, not matched-thread efficiency estimates or repeated-run confidence intervals. Table~\ref{tab:repeats} exposes the available reduced-model variability rather than selecting its fastest repetition. A publication-final timing study should repeat every tier under a common resource-budget protocol and retain all repetitions.

The affine HPROM uses its exact reduced residual Jacobian, whereas HPROM--ANN uses the projected-stiffness modified-Newton matrix described in Section~\ref{sec:adaptive_weights}; both use a microscopic increment tolerance of $10^{-10}$. Their effective constitutive tangents nevertheless account for the equilibrium sensitivity of their respective full residuals. The archived structural runs do not retain microscopic iteration totals. The reported speedup therefore compares these implemented workflows, including their different iteration matrices and derivative costs; it does not isolate a nonlinear-representation gain or quantify the iteration penalty of modified Newton.

\begin{table}[htbp]
 \centering\footnotesize
 \caption{Complete coupon simulations: final-field errors against the nested FOM and archived online wall times.}
 \label{tab:fe2}
 \input{tables/fe2_comparison.tex}
\end{table}
\begin{table}[htbp]
 \centering\small
 \caption{Sequential optimized-run repetitions used for median HPROM timings.}
 \label{tab:repeats}
 \input{tables/timing_repeats.tex}
\end{table}

\subsection{Accuracy--cost trade-off and Newton behavior}
Figure~\ref{fig:cost} and Table~\ref{tab:fe2} show distinct operating points rather than a single universally best model. The linear HPROM gives the smallest displacement error, approximately $0.00565\%$, at a median online time of 67.35 s. The iterative HPROM--ANN reduces this time to 30.14 s with a displacement error of approximately $0.0520\%$. The optimized iterative nonlinear model is therefore about $2.23$ times faster than the \emph{optimized} linear model; comparison against the old linear implementation would overstate this gain. The direct closure reduces the time to 3.79 s and has a displacement error of approximately $0.0232\%$ on this path. Its lower error than the iterative nonlinear model is an observed outcome, not a consequence of a general error ordering.

The ICNN and ICKAN times are 1.73 s and 3.67 s, with displacement errors of approximately $0.425\%$ and $0.387\%$. Their finite-strain structural accuracy is therefore consistent with a useful constitutive approximation, although it is poorer than the reduced microscopic models on this benchmark. Direct stress regression is fastest, and Free also converges. These baselines establish the accuracy and cost achievable on this loading path alongside the constrained models.

\begin{figure}[htbp]
 \centering\includegraphics[width=.93\linewidth]{cost_accuracy.pdf}
 \caption{Online cost versus final displacement error on the common coupon problem. Horizontal segments show the observed range of three optimized HPROM runs; unbarred points are archived single PANN runs. Process and thread counts differ as detailed in Table~\ref{tab:fe2}. The plot does not include offline costs.}
 \label{fig:cost}
\end{figure}

All models require four macroscopic iterations per increment in the archived runs. Consequently, this example provides no empirical evidence that polyconvexity reduced Newton iterations relative to Free or Regression. It establishes that the selected constrained models can be used successfully with the common structural solver, while maintaining their constitutive guarantees. A separate challenge problem would be needed to establish a solver-robustness advantage, and its reference solution would need to be validated independently.

\section{Conclusions}
\label{sec:conclusions}
Paired directional features with constrained trainable powers and determinant exponents provide a practical polyconvex constitutive construction for the anisotropic periodic material studied here. Their isotropic reference derivatives allow exact stress normalization through an affine determinant correction without tying the normalization coefficient to the volumetric barrier stiffness. The proof combines feature convexity in independent minors with core convexity and monotonicity, and includes every correction in the complete energy. Global nonnegative energy is supported separately by a sufficient bound evaluated for the selected parameters. Both a smooth ICNN and an integrated-hat spline-ICKAN achieve approximately $0.65\%$ held-out aggregate stress error and below $0.43\%$ displacement error in the nominal coupon simulation.

Optimized reduced microscopic models provide a complementary accuracy--cost reference: they are more accurate on this coupon, while direct learned constitutive evaluation is generally faster. All compared models converge with the same number of macroscopic iterations per increment. The structural results therefore demonstrate the practical use of the constrained energies alongside their analytical guarantees; they establish neither a Newton-convergence advantage nor a universal accuracy ranking.

\appendix
\section{Rank-one paths and finite-strain energy curvature}
\label{app:finite_strain_curvature}
This appendix supplies the algebra behind the two finite-strain curvature statements in criterion (C5). For a twice differentiable energy, nonnegative curvature along every rank-one perturbation is expressed by the Legendre--Hadamard condition
\begin{equation}
 \partial^2_{\F\F}W(\F)[\bm a\otimes\bm b,\bm a\otimes\bm b]\geq0
 \quad\hbox{for all }\bm a,\bm b\in\R^2 .
 \label{eq:lh_background}
\end{equation}
The argument below shows why the polyconvex representation in Eq.~\eqref{eq:polyconvex_definition} implies this condition.

\paragraph{Determinant along a rank-one path.}
For arbitrary $\F,\bm H\in\R^{2\times2}$, expansion of the two products in the determinant gives
\begin{align}
 \det(\F+t\bm H)
 &=(F_{11}+tH_{11})(F_{22}+tH_{22})
 -(F_{12}+tH_{12})(F_{21}+tH_{21})\nonumber\\
 &=\det\F+t(F_{22}H_{11}+F_{11}H_{22}
 -F_{21}H_{12}-F_{12}H_{21})+t^2\det\bm H.
\end{align}
The coefficient of $t$ is $\cof\F:\bm H$, since
\begin{equation}
 \cof\F=\begin{pmatrix}F_{22}&-F_{21}\\-F_{12}&F_{11}\end{pmatrix},
 \qquad \bm A:\bm B=\sum_{i,j}A_{ij}B_{ij}.
\end{equation}
For $\bm H=\bm a\otimes\bm b$, its entries are $H_{ij}=a_i b_j$, so
\begin{equation}
 \det\bm H=a_1b_1a_2b_2-a_1b_2a_2b_1
 =a_1a_2(b_1b_2-b_2b_1)=0.
\end{equation}
The determinant is therefore affine along a rank-one path:
\begin{equation}
 \det(\F+t\bm a\otimes\bm b)
 =\det\F+t\,\cof\F:(\bm a\otimes\bm b).
 \label{eq:rankone_minor_line}
\end{equation}
If $\F_B=\F_A+\bm H$ and $\F_s=(1-s)\F_A+s\F_B$, with $\bm H=\bm a\otimes\bm b$, then
\begin{equation}
 (\F_s,\det\F_s)
 =(1-s)(\F_A,\det\F_A)+s(\F_B,\det\F_B).
\end{equation}
For $0\leq s\leq1$ and positive endpoint determinants, the whole segment remains admissible. Convexity of $\mathcal P$ therefore yields
\begin{equation}
 W(\F_s)\leq(1-s)W(\F_A)+sW(\F_B).
\end{equation}
For a twice differentiable energy, the restriction to this line consequently has nonnegative second derivative, as required by Eq.~\eqref{eq:lh_background}.

\paragraph{Strain curvature and the chain rule.}
Let $\bm H\in\R^{2\times2}$ be an arbitrary perturbation and let $t$ parametrize $\F(t)=\F+t\bm H$. Dots denote derivatives with respect to $t$, and $\operatorname{sym}(\bm A)=(\bm A+\bm A^T)/2$ denotes the symmetric part of a matrix. Direct multiplication gives
\begin{align}
 \E(t)
 &=\tfrac12\bigl[\F^T\F-\I
 +t(\F^T\bm H+\bm H^T\F)+t^2\bm H^T\bm H\bigr]\nonumber\\
 &=\E(0)+t\operatorname{sym}(\F^T\bm H)
 +\tfrac12t^2\bm H^T\bm H.
\end{align}
Thus $\dot\E(0)=\operatorname{sym}(\F^T\bm H)$ and $\ddot\E(0)=\bm H^T\bm H$. Using the engineering-strain convention of Eq.~\eqref{eq:voigt}, the chain and product rules give
\begin{align}
 \frac{\dd W}{\dd t}&=\ssv\cdot\dot\ee,\nonumber\\
 \frac{\dd^2 W}{\dd t^2}
 &=\sum_{i,j}\frac{\partial s_i}{\partial e_j}\dot e_j\dot e_i
 +\sum_i s_i\ddot e_i
 =\dot\ee^{\,T}\bm D\dot\ee+\Sg:\ddot\E.
\end{align}
Substituting $\ddot\E(0)$ gives
\begin{equation}
 \left.\frac{\dd^2 W(\F+t\bm H)}{\dd t^2}\right|_{t=0}
 =\dot{\ee}(0)^T\bm D\dot{\ee}(0)+\Sg:(\bm H^T\bm H).
 \label{eq:strain_vs_F_curvature}
\end{equation}
Here $\bm D=\partial\ssv/\partial\ee$ is the strain tangent and $\Sg$ is the second Piola--Kirchhoff stress, both evaluated at $t=0$. The first term accounts for the change of stress with strain; the second accounts for the curvature of the strain path, weighted by the current stress. Even if $\bm D$ is positive semidefinite, the stress term can be negative in compression.

\paragraph{An analytical counterexample.}
Consider $W(\E)=\mu\E:\E$, where $\mu>0$ is a constant with units of energy density. One has $\Sg=2\mu\E$. Since
\begin{equation}
 W(\ee)=\mu(e_1^2+e_2^2+e_3^2/2),
 \qquad \bm D=\diag(2\mu,2\mu,\mu),
\end{equation}
the energy is strictly convex in engineering strain. At $\F=\lambda\I$, where $\lambda>0$ is the common in-plane stretch, choose the rank-one perturbation $\bm H=\diag(1,0)$. Then
\begin{equation}
 \dot\ee=(\lambda,0,0)^T,\qquad
 \Sg=\mu(\lambda^2-1)\I,\qquad
 \bm H^T\bm H=\diag(1,0).
\end{equation}
Hence the strain-tangent term is $2\mu\lambda^2$, and the stress term is $\mu(\lambda^2-1)$, giving
\begin{equation}
 \left.\frac{\dd^2 W(\lambda\I+t\bm H)}{\dd t^2}\right|_{t=0}
 =2\mu\lambda^2+\mu(\lambda^2-1)
 =\mu(3\lambda^2-1).
 \label{eq:strain_convex_counterexample}
\end{equation}
At $\lambda=1/2$, the curvature is $-\mu/4$, violating Eq.~\eqref{eq:lh_background} despite convexity in $\E$. This is an analytical counterexample, not a sampled observation about the RVE or a trained model. Compression alone does not force negative curvature: at $\lambda=3/4$, the same expression is $11\mu/16>0$.

Equivalently, direct substitution gives
\begin{equation}
 W(\lambda\I+t\bm H)
 =\frac\mu4\bigl[(\lambda+t)^2-1\bigr]^2
 +\frac\mu4(\lambda^2-1)^2,
\end{equation}
whose second derivative at zero is $\mu(3\lambda^2-1)$. The path is admissible near zero because $\det(\lambda\I+t\bm H)=\lambda(\lambda+t)>0$ for $t>-\lambda$.

\section{Proofs for the paired features and complete energy}
\label{app:convex}
The proof follows the construction used in the main text. We first isolate one stretch--determinant contribution and derive the exponent bounds that make it convex. We then assemble two perpendicular contributions and compute their reference derivative, proving Proposition~\ref{prop:paired_features}. Finally, we compose the resulting features with the monotone convex core and examine $W_{\rm learn}$, $W_{\rm corr}$, $W_{\rm vol}$, and $W_{\rm dist}$ to establish Proposition~\ref{prop:complete_energy}.

\subsection{Joint convexity of the directional building block}
Every contribution in Eq.~\eqref{eq:features} has the form
\begin{equation}
 f(\bm x,J)=\|\bm x\|^aJ^{-b},
 \qquad \bm x=\F\bm d,
\end{equation}
up to a positive constant, with $a=2p$ or $2q$. It is therefore enough to determine when this building block is jointly convex in $(\bm x,J)$. Let $t=\|\bm x\|>0$. Because $f$ depends on $\bm x$ only through $t$, its Hessian separates into a block coupling the radial variable $t$ to $J$ and transverse directions orthogonal to $\bm x$. The radial--determinant block is
\begin{equation}
 \begin{pmatrix}
 a(a-1)t^{a-2}J^{-b} & -ab t^{a-1}J^{-b-1}\\
 -ab t^{a-1}J^{-b-1} & b(b+1)t^aJ^{-b-2}
 \end{pmatrix}.
\end{equation}
For this symmetric $2\times2$ block to be positive semidefinite, its diagonal entries and determinant must be nonnegative. The diagonal entries are nonnegative for $a\geq1$ and $b\geq0$, while the determinant is
\begin{align}
 \det &=ab\bigl[(a-1)(b+1)-ab\bigr]t^{2a-2}J^{-2b-2}\nonumber\\
 &=ab(a-b-1)t^{2a-2}J^{-2b-2}\geq0.
\end{align}
The factor $a-b-1$ gives the upper bound $b\leq a-1$; if this bound is violated, the determinant is negative and the block has curvatures of opposite sign. The remaining, transverse Hessian eigenvalues are $a t^{a-2}J^{-b}\geq0$. Hence $a\geq1$ and $0\leq b\leq a-1$ make the complete Hessian positive semidefinite. Continuous extension gives convexity at $\bm x=0$. For the constitutive evaluation on $\GL$, a nonzero reference direction cannot satisfy $\F\bm d=0$, so this point does not introduce an interior singularity of the directional norm. Substituting $a=2p$ gives $p\geq\tfrac12$ and $0\leq b\leq2p-1$; the same argument with $(q,c)$ establishes all bounds in Eq.~\eqref{eq:featureconstraints}.

\subsection{Proof of Proposition~\ref{prop:paired_features}}
\begin{proof}
\emph{Convexity and objectivity.}
For any fixed learned parameter set, the directions $\bm d_i$ and $\bm d_i^\perp$ are independent of deformation. The maps $\F\mapsto\F\bm d_i$ and $\F\mapsto\F\bm d_i^\perp$ are therefore linear. Composing the convex building block above with these maps, multiplying by the positive factors $1/p_i$ and $1/q_i$, and adding the two contributions proves convexity of $z_i$ in independent $(\F,J)$. A left proper rotation changes $\F\bm d$ to $\bm Q\F\bm d$ without changing its norm, so each contribution is objective.

\emph{Reference value and derivative.}
We now return to the physical relation $J=\det\F$, still holding every learned parameter fixed. At $\E=\bm0$, both directional invariants and $J$ equal one, so the two contributions give $z_i^0=1/p_i+1/q_i$. Moreover, $I_4(\bm d_i)=\bm d_i^T(\I+2\E)\bm d_i$ and $J^2=(1+2E_{11})(1+2E_{22})-4E_{12}^2$ imply $\partial_{\E}I_4(\bm d_i)=2\bm d_i\otimes\bm d_i$ and $\partial_{\E}J|_0=\I$. Applying the product rule to the first contribution gives
\begin{equation}
 \left.\partial_{\E}\frac{I_4(\bm d_i)^{p_i}}{p_iJ^{b_i}}\right|_0
 =\frac1{p_i}\bigl[2p_i\bm d_i\otimes\bm d_i-b_i\I\bigr]
 =2\bm d_i\otimes\bm d_i-\frac{b_i}{p_i}\I.
\end{equation}
The prefactor $1/p_i$ has cancelled the factor $p_i$ produced by differentiating the stretch power. The perpendicular contribution has the same form with $(\bm d_i,p_i,b_i)$ replaced by $(\bm d_i^\perp,q_i,c_i)$. The two directional projectors satisfy $\bm d_i\otimes\bm d_i+\bm d_i^\perp\otimes\bm d_i^\perp=\I$; adding the contributions therefore removes the directional dependence at the reference and gives
\begin{equation}
 \left.\partial_{\E}z_i\right|_0
 =2(\bm d_i\otimes\bm d_i+\bm d_i^\perp\otimes\bm d_i^\perp)
 -\left(\frac{b_i}{p_i}+\frac{c_i}{q_i}\right)\I
 =\rho_i\I.
\end{equation}
In the work-conjugate engineering-strain convention of Eq.~\eqref{eq:voigt}, this tensor gradient corresponds to $\partial_{\ee}z_i|_0=\rho_i(1,1,0)^T$, proving Eq.~\eqref{eq:referencefeatures}.
\end{proof}

\paragraph{Positive affine conditioning.}
The raw feature is converted to the core input by $y_i=(z_i-z_i^{\rm cen})/s_i$, with $s_i>0$ independent of deformation. Writing $\bm X=(\F,J)$,
\begin{equation}
 \partial^2_{\bm X\bm X}y_i=\frac1{s_i}\partial^2_{\bm X\bm X}z_i,
 \qquad
 y_i(\bm X_B)-y_i(\bm X_A)
 =\frac{z_i(\bm X_B)-z_i(\bm X_A)}{s_i}.
\end{equation}
The first identity preserves positive-semidefinite curvature and the second preserves componentwise order. Thus the numerical conditioning applied before the monotone core changes neither property used in the composition proof below.

\subsection{Proof of Proposition~\ref{prop:complete_energy}}
\label{app:complete_energy_proof}

\begin{proof}
\emph{Potential structure, objectivity, and stress symmetry (C1)--(C3).}
Every directional norm and $J$ is unchanged by left proper rotations. The features can also be expressed through $\bm d_i^T\C\bm d_i$ and $\sqrt{\det\C}$. Hence the complete energy is objective, and differentiation with respect to symmetric strain gives a symmetric material stress. Differentiating this scalar energy supplies (C1) and the major symmetry of its strain tangent. Smoothness holds on $\GL$ because $J>0$, nonzero material directions have nonzero deformed length, and both cores are $C^2$. Because this argument uses left rotations only, it does not impose a material symmetry group.

\emph{Reference normalization (C4).}
Equation~\eqref{eq:energy_reference_value} shows that $W_{\rm learn}$ sets the reference energy, and Eq.~\eqref{eq:energy_reference_stress} shows that $W_{\rm corr}$ cancels the learned reference stress. The calculations for $W_{\rm vol}$ and $W_{\rm dist}$ in Section~\ref{sec:energy_admissibility} show that both growth terms have zero value and zero strain gradient at the reference. Thus the complete energy satisfies both reference conditions.

\emph{Polyconvexity (C5).} Let $\bm G\in\R^{2\times2}$ and $j>0$ be independent. Define $\mathcal P(\bm G,j)$ by the right-hand side of Eq.~\eqref{eq:energy}, replacing $\bm z$ by $\bm z(\bm G,j)$, $J$ by $j$, and $\tr\C$ by $\|\bm G\|_F^2$. For two such pairs $\bm X_1=(\bm G_1,j_1)$, $\bm X_2=(\bm G_2,j_2)$ and $0\leq t\leq1$,
\begin{align}
 H\!\left(\bm z(t\bm X_1+(1-t)\bm X_2)\right)
 &\leq H\!\left(t\bm z(\bm X_1)+(1-t)\bm z(\bm X_2)\right)\nonumber\\
 &\leq tH(\bm z(\bm X_1))+(1-t)H(\bm z(\bm X_2)).
 \label{eq:convex_composition_proof}
\end{align}
The first inequality uses feature convexity and core monotonicity; the second uses core convexity. Therefore $W_{\rm learn}$ is convex up to its constant reference subtraction. The correction $W_{\rm corr}=-r(j-1)$ is affine for either sign of $r$, while $W_{\rm vol}$ and $W_{\rm dist}$ are convex in independent $(\bm G,j)$ because their nonaffine parts are positive multiples of $-\log j$, $(j-1)^2$, and $\|\bm G\|_F^2$. Thus $\mathcal P$ is convex and $W(\F)=\mathcal P(\F,\det\F)$, establishing Eq.~\eqref{eq:polyconvex_definition}.

\emph{Growth (C6).}
Convexity of $H$ gives its supporting-plane inequality at $\bm z^0$:
\begin{equation}
 H(\bm z)\geq H(\bm z^0)+\sum_i h_i(z_i-z_i^0)
 \geq H(\bm z^0)-\sum_i h_i z_i^0.
\end{equation}
The second inequality uses $h_i,z_i\geq0$. Hence $W_{\rm learn}$ has a deformation-independent lower bound and cannot cancel the analytic growth terms. As $J\to0^+$, the logarithmic part of $W_{\rm vol}+W_{\rm dist}$ diverges while $W_{\rm corr}$ remains bounded. As $J\to\infty$, the positive quadratic part of $W_{\rm vol}$ dominates every potentially negative linear or logarithmic term. Finally, if $J$ remains in a compact subset of $(0,\infty)$ while $\|\F\|_F\to\infty$, the term $\varepsilon\|\F\|_F^2/2$ in $W_{\rm dist}$ diverges and all determinant-only terms remain bounded. These are the three stated growth limits.
\end{proof}

\section{Sufficient lower bound for nonnegative energy}
\label{app:nonnegative}
Define $k_i=1/p_i+1/q_i$ and $\eta_i=\rho_i/k_i$. Write $t=\|\F\bm d_i\|^2$, $u=\|\F\bm d_i^\perp\|^2$. Hadamard's determinant inequality gives $tu\geq J^2$. Since each feature is increasing in $t,u$, its minimum at fixed $J$ occurs on $tu=J^2$. Minimizing
\begin{equation}
 \frac{t^{p_i}}{p_iJ^{b_i}}+\frac{J^{2q_i-c_i}t^{-q_i}}{q_i}
\end{equation}
gives $t^{p_i+q_i}=J^{2q_i+b_i-c_i}$ and hence $z_i\geq k_iJ^{\eta_i}$. This is a separate lower bound for each feature; simultaneous attainability for all directions is not assumed. It therefore remains valid after summing the supporting-plane inequalities.

Together with $\tr\C/2\geq J$ and the supporting plane of $H$, this feature bound gives
\begin{equation}
 W\geq g(J):=\sum_i h_i k_i[J^{\eta_i}-1-\eta_i(J-1)]
 +(\alpha+\varepsilon)(J-1-\log J)+\frac\beta2(J-1)^2.
 \label{eq:lowerbound}
\end{equation}
Because $g(1)=g'(1)=0$, it remains to establish $g''(J)\geq0$ for every $J>0$. Differentiation gives
\begin{equation}
 J^2g''(J)=\alpha+\varepsilon+\beta J^2+\sum_i c_i^*J^{\eta_i},
 \qquad c_i^*=h_i k_i\eta_i(\eta_i-1).
\end{equation}
Only terms with $0<\eta_i<1$ can have negative coefficients. If $c_-=\sum_{c_i^*<0}|c_i^*|$, the two inequalities
\begin{align}
 \alpha+\varepsilon+\sum_{\eta_i<0}c_i^*-c_-&\geq0,\\
 \beta+\sum_{\eta_i\geq1}c_i^*-c_-&\geq0
\end{align}
are sufficient on $0<J\leq1$ and $J\geq1$, respectively. For the first region, $J^{\eta_i}\leq1$ for negative-coefficient terms and $J^{\eta_i}\geq1$ when $\eta_i<0$. For the second region, divide $J^2g''$ by $J$: all negative-coefficient powers then satisfy $J^{\eta_i-1}\leq1$, while $J^{\eta_i-1}\geq1$ for $\eta_i\geq1$ and $\beta J\geq\beta$. Dropping the remaining nonnegative contributions gives the stated bounds. Their failure is inconclusive.

The selected parameters require a sharper check. The audit partitions $[10^{-4},10^4]$ into 4000 logarithmically spaced intervals. On each interval $[J_L,J_R]$, a positive power term is bounded below by the smaller endpoint value, whereas a negative-coefficient term is bounded below by $c_i^*J_R^{\eta_i}$, since its exponent lies in $(0,1)$. The constant and quadratic terms are bounded below by $\alpha+\varepsilon$ and $\beta J_L^2$. Below the finite interval, the nonnegative negative-exponent terms increase and the magnitudes of the negative-coefficient terms decrease toward $J=0$. Above it, factoring out $J^{\eta_-}$ with $\eta_-=\max_{c_i^*<0}\eta_i<1$ yields a lower bound whose retained positive powers increase. These endpoint and tail inequalities cover every $J>0$, not only the grid points. Positive margins establish the sufficient analytical argument for the saved parameters to the accuracy of its floating-point evaluation; random energy samples alone would not establish it.
